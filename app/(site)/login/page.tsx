import { RouteStub } from "@/components/RouteStub";

export default function Page() {
  return (
    <RouteStub
      title="Log in"
      blurb="Account sign-in is scaffolded as a route for now. Can switch to an external app URL later."
    />
  );
}
