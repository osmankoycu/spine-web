import { RouteStub } from "@/components/RouteStub";

export default function Page() {
  return (
    <RouteStub
      title="Request a demo"
      blurb="Scaffolded as a route for now. Can switch to an on-page modal later without touching the Header."
    />
  );
}
